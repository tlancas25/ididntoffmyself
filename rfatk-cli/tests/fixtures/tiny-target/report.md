# Trial report (raw bundle) — tiny-target

**Status:** pending — agents not yet run.

Populated by the synthesizer after specialist agents complete
(run `rfatk attack <this-folder>` or `rfatk report <this-folder>`).
